# foundit_web_app
FoundIt is a full-stack, capstone-level web app helping users find lost items. It uses a decoupled architecture with a vanilla JS frontend and a Node.js/Express REST API. Key features include PostgreSQL storage, JWT sessions, bcrypt hashing, and admin role overrides.
